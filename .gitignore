__pycache__/
*.pyc

# Static exports are build artifacts, regenerated at release. Committing
# one would create a second document to keep current, and the whole
# point is that there is only ever one.
*_prod.html

# Per-user session permissions, never shared.
.claude/settings.local.json
